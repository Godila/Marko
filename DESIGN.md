# МАРКО · Дизайн-система UI

Источник правды по стилю консоли оператора. Любая доработка UI — сначала сверка с этим
файлом, потом код. Классы и токены живут в `ui/src/marko.css`, словари и хелперы —
в `ui/src/App.jsx`, знак — `ui/logo-marko.html` + `Mark`-компонент.
Прод-консоль: marko.adel-factory.ru.

---

## 1. Принципы

1. **Удобство = очередь решений.** Каждый экран отвечает оператору на вопрос «что горит
   и что нажать», а не «какие есть таблицы». Разделы = рабочие процессы, не сущности БД;
   навигация — два пространства: «Оборот и маркетплейсы» (Обзор, Вывод, Возвраты,
   Журнал, Трассировка, Идентификаторы WB, Нанесение КиЗ) и «Нацкаталог» (Каталог НК, Справочники).
2. **Кармин — только для срочного.** Один акцентный цвет (`--stamp`) на дедлайны,
   аномалии и красные статусы. Нигде больше. Декоративного красного нет.
3. **Статусы — человеческими словами.** «принят ЧЗ», а не `CHECKED_OK`. Системный код —
   второй слой (в дроверах, подзаголовках). Словари — в разделе 8, не придумывать заново.
4. **Необратимое — через подтверждение.** Любое действие, которое нельзя отменить
   (подача в ЧЗ, удаление), — модалка с последствиями и монодеталью.
5. **Сырой JSON — не интерфейс.** Данные показываются таблицами и бейджами; JSON —
   в боковом дровере («Состав», карточка события журнала).
6. **Автоматика прозрачна и честна.** Пульс показывает возраст последнего прогона;
   нет метки = «ещё не было»/устарело, а не зелёная точка по умолчанию.
7. **Тишина.** Никаких декоративных анимаций, градиентов, эмодзи, теней сверх
   разрешённых. Плотность и спокойствие важнее эффектов.

## 2. Бренд и тон

- Имя: **МАРКО** (МАРкировка + КОды). В интерфейсе — всегда «МАРКО», без «платформа».
- Язык: русский, sentence case. Адрес — на «вы» избегаем, обращения безличные.
- Кнопка называет результат: «Собрать вывод», а не «Отправить». Тост повторяет глагол
  кнопки: «Собрать вывод» → «Создан черновик LK_RECEIPT №24».
- Ошибки говорят, что делать: «Заполните номер и дату», а не «Ошибка».
- Пустые состояния — приглашение к действию: заголовок жирным + одна строка что делать.
- Терминология домена: КМ/«код маркировки», «вывод из оборота», «принят ЧЗ», ПВЗ,
  батч/карточка НК. Не «товар», не «заявка».

## 3. Палитра

| Токен | Значение | Применение |
|---|---|---|
| `--paper` | `#F1F4F3` | фон приложения |
| `--surface` | `#FFFFFF` | карточки, таблицы |
| `--surface-2` | `#F7F9F8` | вложенные зоны, hover строк |
| `--ink` | `#17242B` | основной текст, primary-кнопка |
| `--muted` | `#5E6B72` | вторичный текст |
| `--faint` | `#93A0A6` | подписи, заголовки таблиц |
| `--line` / `--line-strong` | `#DDE3E2` / `#C4CDCC` | рамки / рамки контролов |
| `--stamp` | `#C81E36` | срочное: дедлайны, аномалии, «М» знака |
| `--stamp-bg` | `#FBEDEF` | подложка красных бейджей, горячих строк |
| `--go` / `--go-bg` | `#1E7A4C` / `#E9F4EE` | успех: принят ЧЗ, опубликовано |
| `--wait` / `--wait-bg` | `#9A5B00` / `#FBF3E2` | ожидание: модерация, «к выводу» |
| `--info` / `--info-bg` | `#2759B7` / `#EBF1FA` | в работе: подан, «к возврату» |
| `--rail` / `--rail-2` | `#0E181E` / `#152430` | тёмная панель, её hover |
| `--rail-ink` / `--rail-mut` | `#E9EFEF` / `#8DA0AA` | текст рельса |

Правила: новый цвет не вводить. Если нужен новый смысл — сначала проверить, не покрывается
ли он пятью статусными парами (stamp/go/wait/info/grey). На тёмном рельсе кармин светлеет
до `#E42B47` (см. `Mark`). Контраст текста к фону — не ниже 4.5:1.

## 4. Типографика

| Роль | Шрифт | Использование |
|---|---|---|
| Дисплей | `Unbounded` 500–600 | только wordmark, `h1` разделов, крупный логотип |
| Интерфейс | `Golos Text` 400–600 | всё остальное: текст, кнопки, заголовки карточек |
| Данные | `JetBrains Mono` 400–500 | КМ, uuid, даты, счётчики, квоты, `.num` |

- `h1` раздела: 21 px Unbounded 500. Заголовок карточки `h2`: 14 px Golos 600.
- База 14 px / 1.5. Таблицы 13 px, мелкие таблицы 12.5 px.
- Цифры в таблицах — `font-variant-numeric: tabular-nums` (класс `.num`).
- Новые шрифты не подключать; Google Fonts линк живёт в `ui/index.html`.

## 5. Геометрия

- Радиусы: карточки `--r: 10px`, контролы `--r-s: 6px`, подложка favicon/инверсий 14–20 px.
- Отступы кратны 4: внутри карточек 16/18, между карточками 22, поля контента 26/34.
- Тень одна: `--shadow` (0 12px 40px rgba(14,24,30,.18)) — только модалка, тосты, дровер.
- Линии-разделители 1px `--line`; рамок двойной толщины нет; box-shadow у карточек нет.

## 6. Каркас

- Десктоп: тёмный рельс 232 px (sticky) + контент на всю ширину окна — потолка
  max-width НЕТ (инциденты 17–18.09: 1160 → 1440 → всё равно пусто на широких
  мониторах; гриды fit+colgroup растягиваются, флекс-колонка забирает экран).
- `<960px`: рельс скрыт, горизонтальный скролл-нав в `.topnav`, `.grid2` и пульс — в 2 колонки,
  `.formgrid` — в 1. Таблицы не ломаем: горизонтальный скролл в `.twrap`.
- Навигация: иконка + подпись + счётчик `.cnt` (только то, что требует действия:
  «к выводу», «к возврату», активные батчи, аномалии). Счётчик активного пункта — кармин.
- Рельс — два пространства с mono-заголовками `.nav-cap` («Оборот и маркетплейсы» /
  «Нацкаталог»); `<960px` topnav плоский, без заголовков. Реквизиты эмиттера LK_RECEIPT
  (ИНН/ФИАС/первичка) — карточка раздела «Вывод из оборота», не справочник.

## 7. Компоненты (классы `marko.css`)

- **Карточка** `.card` + `.card-h` (заголовок + hint справа) + `.card-b`. Между карточками
  22px (`card + card`).
- **Кнопки** `.btn` (`pri` — graphite-заливка, основной призыв; `sm`; disabled 0.45).
  Одна primary-кнопка на блок. Опасные действия — обычная `pri` + модалка.
- **Бейджи** `.bdg` `grey|blue|amber|green|red` — точка + слово. Единственный способ
  показывать статус. Красный бейдж = требует человека.
- **Чипы-фильтры** `.chip` (`aria-pressed`), `.chip.alert` для аномалий; счётчик внутри `.n`.
- **Дедлайны** `.cd` `ok(>48ч)|warn(≤48ч)|danger(≤24ч)|over(просрочен)` — пороги менять
  нельзя, они привязаны к TG-алертам (48 ч). Строка целиком `.rowhot` при danger/over.
- **Таблицы** `.t` / `.t.small`; KМ — `.km` + кнопка-копия; действия — `.actions` справа;
  числовые колонки — `.num`.
- **Табличные формы (гриды разделов)** — всегда `table.t(.small).fit` + `<colgroup>`:
  - ширины заданы **ВСЕМ колонкам** (`<col style={{width:N}}>`), колонок без width
    НЕ допускается: в fixed layout такая колонка забирает весь избыток и
    раздувается до 1000+ px на широких мониторах (инцидент 18.09);
  - при ширине контейнера больше суммы ширин таблица масштабируется
    **пропорционально** (поведение браузера), при меньшей — держит сумму ширин и
    уходит в горизонтальный скролл `.twrap` (без `minWidth`, сумма неявный минимум);
  - сумма ширин ≈ 1236 px = контент на ноутбуке 1536 (превью — 1204 = контент
    wide-модалки); колонки статусов — под самые длинные подписи словарей;
  - бейджи в гридах `table.t .bdg{max-width:100%}` — не наезжают на соседние
    колонки, перенос только по словам (не рвать слова);
  - длинные значения — `td.ell` (однострочник + `title`, клик раскрывает), коды —
    `td.ell.mono`;
  - действует на ВСЕ полосные таблицы разделов: журнал, вывод, документы ЧЗ,
    возвраты, карточки батчей, превью импорта, декларации, производители, правила;
  - сортируемые заголовки: `th.sortable` + `aria-sort`, стрелка ▲/▼ только у активной
    столбца (faint, 9px), клик по тому же столбцу меняет направление, по новому — `desc`;
    строки без даты сортировки всегда внизу;
  - таблицы-детали в дроверах (label→value, `.t.small` без thead) — НЕ гриды,
    авторазметка; клик по строке грида открывает дровер-карточку сущности, действия
    живут в карточке, а не в строке (кроме коротких списков действий ≤2 кнопок).
- **KPI-полоса** `.strip`/`.kpi`: число + подпись; цвет числа = смысл (warn/bad/good).
- **Баннер** `.banner` — одно срочное сообщение раздела с иконкой; не более одного.
- **«Требует решения»** `.act` + цветная полоса `.bar` (urg/warn/info) + кнопка действия
  в строке. Порядок: дедлайны → деньги/вывод → черновики → аномалии.
- **Лента** `.feed`: время (mono) + точка-цвет + одна строка смысла.
- **Рельса этапов** `.path` (трассировка): одна линия времени, фаза = участок
  системы (mono-eyebrow `ЧЕСТНЫЙ ЗНАК / WILDBERRIES / ПОСЛЕ ПРОДАЖИ`), точка = состояние
  этапа: факт — заливка ink, «ждёт нашего действия» — wait, «не наступило» — полая,
  «ретроспективно недоступно» — пунктир. Один цвет с смыслом (wait), остальное —
  монохром. Узкий экран — горизонтальный скролл самой рельсы, страница не ширеется.
- **Пульс** `.pulse` (сигнатура): 4 vitals на тёмном + ЭКГ-линия. Витал = название,
  возраст последнего прогона, серая строка пояснения; пороги warn: WB 13 ч, signer 10 мин,
  НКМТ 20 мин, возвраты 90 мин.
- **Конвейер** `.pipeline`/`.stage`: статичные стадии со счётчиками, клик = фильтр.
- **Дровер** `.drawer` (520 px) — детали/JSON, закрытие по фону и Esc.
- **Модалка** `.veil`/`.modal`: заголовок-вопрос, текст-последствия, `.mono-s` деталь,
  «Отмена» + глагол.
- **Широкая модалка** `.modal.wide`: `min(1240px, 96vw) × min(860px, 90vh)`, секции
  `.modal-h` (шапка с «Закрыть») + `.modal-b` (скролл-тело: `.wide-fill` → `.wide-scroll`)
  + `.modal-f` (футер со статами и кнопками). Для широких таблиц (предпросмотр
  импорта НК): `table.t.fit` + colgroup, как в журнале. Закрытие — фона, Esc, кнопка.
  Плотные таблицы — обычные модалки/дроверы не растягиваем.
- **Тосты** `#toasts`: заголовок + деталь, 4.5 с; `warn`/`bad` по цвету точки.
- **Пусто** `.empty`: жирный заголовок + строка-приглашение.
- **Формы** `.field`/`.frow`/`.formgrid`, поиск `.search`. Подсказки под полем —
  12px muted. Фокус всегда виден (`:focus-visible`, info-обводка).

## 8. Словари статусов (не расширять молча)

Ключ → подпись → цвет. Источник: `App.jsx` (`ITEM_STATES`, `DOC_STATUS`, `CARD_STATUS`,
`BATCH_STATUS`).

- Документ ЧЗ: черновик·grey → подписывается/подан·blue → принят ЧЗ·green → ошибка·red.
- Карточка НК: новая·grey → подана·blue → модерация/ждёт подписи·amber → подписывается·blue
  → опубликована·green; ошибка/ошибки/ошибка подписи·red.
- КМ: новый·grey → к выводу·amber → выведен·green; к возврату·blue → возвращён·green;
  аномалии·red: возврат без продажи · повторный возврат · неопознанное событие.
  Плашка «выведен» НИКОГДА не утверждает факт раньше ЧЗ: у позиций с нашим документом
  она расшифровывает этап — «вывод: черновик №N»·amber → «вывод: подан №N»·blue →
  «выведен · №N»·green (только после CHECKED_OK; ошибка·red). Вывод чеком WB —
  «выведен (WB)»·green без номера. Колонка ЧЗ рядом — факт госреестра: пары
  «черновик + в обороте» и «выведен + выбыл» всегда согласованы (инцидент 23.09:
  «выведен» + ЧЗ «в обороте» противоречили — слово врёт, факт нет).
  Клиентский возврат (финансовый след sales «R…», событие `wb_sales/client_return`):
  по непроведённому выводу снимает обязательство («к выводу» → «возвращён» — код
  в обороте, товар у продавца), после нашего вывода ставит «к возврату» (LP_RETURN);
  вывод WB — только наблюдение, состояние не меняет.
  «Вторичная продажа» — только легаси-строки до 22.09.2026: перепродажа возврата
  (возврат/невыкуп → WB перевыставляет единицу → новая продажа) теперь штатный
  цикл, обязательство вывода идемпотентно продаже — «к выводу» остаётся «к
  выводу», «выведен» остаётся «выведенным» (событие пишется, виден в трассировке).
  Легаси-строки с retired в ЧЗ вычищает «Обновить статусы ЧЗ» → «выведен (WB)».
  Разбор аномалии (карточка по клику на строку) — событие
  `manual/resolve`: переводит код в штатное состояние и пишет аудит (кто/когда/
  почему — след в таблице событий); новых статусов не вводит. (FBW-строки excise
  в журнал не попадают — они вне контура FBS.)
- КИЗ по Честному ЗНАКу (cises/info, колонка «ЧЗ» и карточка КМ): в обороте·blue ·
  выбыл·green · списан·grey · «—» = не проверялся. Обновление — кнопка «Обновить
  статусы ЧЗ» (все коды) или «Проверить в ЧЗ» (один код); «выбывшие» без нашей
  поданной заявки на вывод автоматически помечаются «вывел WB» (событие cz/withdraw).
- Декларация ЧЗ (rd/list, таб «Декларации»): действует·green · истекает ≤60 дн·amber
  («истекает ДД.ММ.ГГГГ») · не действует/иной статус·red (подписью из ЧЗ) ·
  не проверялась·grey («Проверить в ЧЗ» обогащает: статус, срок, ТН ВЭД-список,
  техрегламенты, изготовитель).
- Батч: новый·grey → подача·blue → модерация·amber → подпись·blue → опубликован·green;
  частично·amber; ошибка·red.
- Контур заказа WB (не статус, атрибут): FBS · фулфилмент·blue (наша отгрузка и наш
  вывод из оборота) · FBO · зона WB·grey (остатки на складе WB — вывод/возврат делает
  WB, от нас ничего не зависит). Источник: реестр wb.orders.delivery_type (точный,
  но слеп назад — выкупленный уходит из снапшота); фолбэк — склад sales-строки
  («Склад WB РФ» = FBO). Подпись в журнале под заказом, бейдж в возвратах
  покупателей, мини-подпись в невыкупах.

Новое состояние — сначала сюда, потом в код; подпись без капса, без кодов `PENDING_*`
в бейджах.

- Трассировка КМ (раздел «Трассировка», не статусы — система-источник события):
  МАРКО·grey · Честный знак·blue · Wildberries·amber. Красный системам не выдаётся
  (система ≠ «требует человека»). Точка ленты `.feed` по виду события: продажа —
  info, возврат покупателя — wait, вывод/возврат в оборот — go, остальные —
  нейтральная. Статусы закрепления sgtin на WB (словарь `SGTIN_DECISION`,
  официальный справочник WB orders/meta): подпись человеческая, неизвестное
  решение — как есть (mono), выдумывать нельзя. Статус заказа WB по ленте
  (order-feed, словарь `WF_STATUS`): оформлен·blue → куплен·green →
  возвращён·amber → возвращён (брак)·red → отменён·grey (+тип отмены подписью).
- Справочник идентификаторов WB (раздел «Идентификаторы WB»). Тип ключа
  (rid заказа/продажи · ID сборочного задания · КМ · GTIN · nmId · поставка)
  — атрибут, не статус: подпись без цвета (`KEY_TYPES`), бейджи внутри раздела —
  только из существующих словарей (`SGTIN_DECISION`, `WF_STATUS`, `ITEM_STATES`,
  `CARD_STATUS`, `ORDER_LOOKUP_STATUS`), новых статусов раздел не вводит.
  Закреплённые КиЗ по заказу — живой orders/meta с кэшем 10 минут; источник
  подписан («live · возраст» / «кэш · возраст»), WB недоступен — пустое
  состояние с причиной, локальные срезы живут. КМ — переход в Трассировку.
  Ввод ID заказа в поиске журнала ведёт сюда.
- Нанесение КиЗ (раздел «Нанесение КиЗ»). Печать термо-этикетки 58×40 мм с
  GS1-DataMatrix ПОЛНОГО КиЗ (01… 21… 91… 92…, из закреплений WB): превью —
  бумажный макет в мм (имя слева 9pt, матрица 23×23, человекочитаемые строки
  7pt — те же модули, что уходят в PDF). Атрибуты без бейджей: источник имени
  (карточка НК / журнал ЧЗ / GTIN) и решение WB — из `SGTIN_DECISION`
  подписью. Блокировка печати (выбыл/списан/не найден + короткий код без
  криптохвоста) — текст с причиной в кармин и погашенная кнопка, не новый
  статус. Кнопки-мосты «Этикетка» — в строках закреплений «Идентификаторов
  WB» и «Трассировки». Словари раздел не расширяет.

## 9. Знак и favicon

- «Матрица-М»: сетка 9×9, L-искатель слева/снизу (цельные бары), тайминг сверху/справа
  через модуль, в центре «М» 5×5. Эталон и генератор — `ui/logo-marko.html`.
- Правило цвета: «М» всегда кармин, рамка и тайминг — монохром (графит на светлом,
  `--rail-ink` на тёмном; кармин на тёмном `#E42B47`). Акцент один.
- В UI знак ставится только через `Mark`-компонент (`App.jsx`); размеров: рельс 34,
  вход 40 (светлая пара `#17242B`+`#C81E36`), топнав 26.
- Favicon — упрощённый (без тайминга) на светлой подложке `#F1F4F3`, `ui/public/favicon.svg`.
  Мини-версия живёт только в favicon: в 16 px тайминг — шум.
- Запрещено: круглый знак с галочкой (клон «Честного знака»), градиенты, обводки,
  перенос «М» в другой цвет.

## 10. Движение

- Разрешено: ЭКГ в пульсе, пульс-кольца `.pdot`, появление тостов, hover-переходы 0.12 s.
- Автообновление данных 60 с + ручной «Обновить»; счётчик «обновлено N с назад» в шапке.
- `prefers-reduced-motion: reduce` глушит всю анимацию (правило уже в css — не удалять).
- Скролл-анимации, параллакс, спиннеры на каждый чих — нет.

## 11. Текст и форматы

- Даты/время: `ДД.ММ ЧЧ:ММ` (`fmtD`); относительный возраст: «N мин назад» / «N ч назад»
  (`fmtAgo`); дедлайны: «18 ч» / «6 дн» / «просрочен» (`fmtLeft`).
- Деньги: `4 590 ₽` (`rub`, неразрывные пробелы); ИНН/uuid/КМ — mono.
- Кавычки «ёлочки», тире —, многоточие …. Числа с счётчиком: «3 КМ готовы», «1 документ(а)»
  допустимо при динамике; лучше переформулировать без скобок.
- Вписки кодов (`ANOMALY_RESALE`, uuid) — только mono и как второй слой.

## 12. Как расширять

- **Новый раздел**: пункт в `NAV_SECTIONS` соответствующего пространства (иконка из
  набора `I`, без эмодзи) → вьюха-функция с `Head` + `Sync`; данные грузить в
  `useEffect([ctx.tick])`; действия — через `ctx.confirm`
  и `ctx.notify`; обновление после мутации — `ctx.bump()`. Роутер — обычный if по `view`.
- **Новый статус**: словарь раздела 8 → бейдж-цвет по смыслу → ничего красного без причины.
- **Новый контрол**: сначала искать класс в `marko.css`; дубликаты стилей не плодить;
  inline-стили — точечные поправки, не макет.
- **Мок-проверка**: перед выкатом прогонять сборку в браузере с перехватом `/v1/*`
  (playwright route) на оба состояния — пустое и наполненное; консоль должна быть без ошибок.
- Тесты бэкенда обязательны для новых эндпоинтов (см. `test_pulse_aggregate` как образец).

## 13. Чек-лист перед коммитом UI

- [ ] Кармин потрачен только на срочное?
- [ ] Все статусы из словарей, подписи человеческие?
- [ ] Необратимое — через confirm-модалку?
- [ ] Пустые состояния с приглашением, ошибки с «что делать»?
- [ ] Грид-таблица: `.fit` + colgroup, ширины ВСЕМ колонкам (флекс-колонка
      раздувается — см. §7)?
- [ ] 960 px не ломается, таблицы скроллятся, фокусы видны?
- [ ] `prefers-reduced-motion` учтён, новых анимаций нет?
- [ ] `npm run build` зелёный, консоль браузера чистая?
- [ ] Коммит запушен в origin.
